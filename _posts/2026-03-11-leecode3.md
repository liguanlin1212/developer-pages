---
layout: post
title: "leecode 字符串 3道"
date: 2026-03-11
---

今天完成的任务：

##完成leecode三道关于字符串的算法题

相关笔记保存在飞书中：

- 交替合并字符串  https://gcnqbk3mr3ut.feishu.cn/docx/ChMpdxpfHoHYddxx5ahcR23Ynge
  1. 思路：
      1. 当word1和word2中有一个未遍历完，则继续运行；
      2. 当word1未遍历完，则push_back;
      3. 当word2未遍历完，则push_back;
 
  2. word1.length() 和 word1.size()的使用；
  3. 对于空字符串，不能通过下标访问并修改，需要使用push_back;
  4. 注意 在循环中定义变量，每一轮循环都会重置；

- 找不同  https://gcnqbk3mr3ut.feishu.cn/docx/Co6yd1pVRoLOCVxgjS7cpr0mnZc
  1. 学习 “异或XOR” 的使用；
  2. 通过手动构建字典，统计出现过的字母；
  3. 通过ASCII码直接计算出多出来的字母；
  4. 考虑重复字符；
  5. 函数要考虑所有路径下的 return；

- 找出字符串中第一个匹配项的下标  https://gcnqbk3mr3ut.feishu.cn/docx/EkrsdO9Fpo0ujSx8fADcwsB3nXf
  1. 嵌套for(char ch:haystack)、for(char c:needle) 会导致外层停留在第一个字符就退出函数；
  2. 通过嵌套 for(int i=0;i+needle.size()<=haystack.size())、 for(int j=0;j<needle.size())+if(haystack[i+j]!=needle[j])实现记录上一轮匹配位置；
  3. 学习KMP算法；
  4. 库函数调用；
